---
# Sensitive variables (DO NOT COMMIT TO VCS)
mailserver_user: "REPLACE_WITH_MAILSERVER_USERNAME"

# SSH public key for the new user (required by base_os role)
# Use ssh-ed25519 or ecdsa-sha2-nistp256 for better security
new_user_public_key: "ssh-ed25519 REPLACE_WITH_YOUR_SSH_PUBLIC_KEY user@example.com"
