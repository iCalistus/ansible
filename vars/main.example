---
# Main variables for mail deployment
# NOTE: Secrets (API tokens, passwords) must be defined in vars/secrets.yml
# (see vars/secrets.example) and the real vars/secrets.yml must be gitignored.
# Configuration values like usernames, hostnames, etc. belong here in vars/main.yml.
mail_hostname: mail.example.com
mail_domain: example.com
iredmail_version: 1.6.8
admin_email: admin@example.com
mailserver_user: REPLACE_WITH_MAILSERVER_USERNAME
